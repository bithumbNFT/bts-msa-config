spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        encrypt:
          enabled: false
        git:
          uri: https://github.com/baekinjun/msa-configurations
encrypt:
  key: bts-key